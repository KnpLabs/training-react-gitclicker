import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  cssVariables: true,
  palette: {
    primary: {
      main: '#556cd6',
    },
    background: {
      default: '#FAFAFA',
    },
  },
})

export default theme
